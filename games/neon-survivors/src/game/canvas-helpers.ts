/**
 * Canvas2D drawing helpers — re-exported from @survivors/core.
 * Kept as a local barrel for backward compatibility.
 */
export { drawJoystick, drawParticles, drawFloatingText, applyCameraToContext } from '@survivors/core';
