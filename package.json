{
  "name": "survivors-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=games/neon-survivors",
    "build": "npm run build --workspace=games/neon-survivors",
    "preview": "npm run preview --workspace=games/neon-survivors"
  }
}
